# Execution Plan

1. Add Prisma migration/model for platform sessions.
2. Add backend auth-session service, login/register session issuance, logout,
   and JWT enforcement.
3. Add Flutter auth device info provider, send device payload on login/register,
   and clear local state on remote auth rejection.
4. Update docs, harness records, and validation matrix.
5. Validate Prisma, backend tests/build, Flutter analyze/tests, and diff check.

