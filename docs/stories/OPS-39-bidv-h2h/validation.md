# OPS-39 - Validation

Local proof requires Prisma format/validate/generate, Nest focused/full tests
and build, fresh/upgraded scratch migration, Flutter focused/full tests and
analyze, Go tests, Caddy validation/default-deny smoke, the final affected-
consumer wrapper, secret scan, and visual plus structural PDF verification.

External gaps: BIDV-produced OpenPGP fixture, IP allowlist confirmation (mTLS is
explicitly disabled),
DNS/tunnel routing, staging 10 QPS evidence, staging reconciliation across all
payment consumers and production release approval.
