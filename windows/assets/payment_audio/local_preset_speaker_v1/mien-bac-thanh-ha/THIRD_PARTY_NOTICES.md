# Payment speaker voice preset

This immutable PCM WAV pack was supplied from the approved local OPS-43 TTS sample set. It contains no model, TTS runtime, or network dependency. Exact asset hashes and audio policy are in `manifest.json`.
