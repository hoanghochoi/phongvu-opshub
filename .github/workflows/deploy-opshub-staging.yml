name: Deploy OpsHub Staging

on:
  push:
    branches:
      - staging
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: opshub-staging-deploy
  cancel-in-progress: false

env:
  OPSHUB_PUBLIC_BASE_URL: https://opshub-staging.hoanghochoi.com
  OPSHUB_API_BASE_URL: https://opshub-staging.hoanghochoi.com/api
  OPSHUB_DOWNLOAD_PUBLIC_BASE_URL: https://opshub-staging.hoanghochoi.com
  OPSHUB_REMOTE_APP_DIR: /home/hhh/phongvu-opshub-staging
  OPSHUB_ENV_FILE: /srv/opshub-staging/env
  OPSHUB_SSD_ROOT: /srv/opshub-staging
  COMPOSE_PROJECT_NAME: opshub_staging

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      version_name: ${{ steps.version.outputs.version_name }}
      windows_build_name: ${{ steps.version.outputs.windows_build_name }}
      version_code: ${{ steps.version.outputs.version_code }}
      apk_name: ${{ steps.version.outputs.apk_name }}
      windows_zip_name: ${{ steps.version.outputs.windows_zip_name }}
      windows_installer_basename: ${{ steps.version.outputs.windows_installer_basename }}
      windows_installer_name: ${{ steps.version.outputs.windows_installer_name }}
      windows_checksum_name: ${{ steps.version.outputs.windows_checksum_name }}
      release_notes: ${{ steps.version.outputs.release_notes }}
    steps:
      - name: Compute staging release version
        id: version
        shell: bash
        run: |
          set -euo pipefail
          version_name="$(date -u +%Y.%m.%d).${GITHUB_RUN_NUMBER}"
          windows_build_name="$(date -u +%Y).$(date -u +%-m).$(date -u +%-d)"
          version_code="$((200000 + GITHUB_RUN_NUMBER))"
          apk_name="phongvu-opshub-staging-v${version_name}+${version_code}.apk"
          windows_zip_name="phongvu-opshub-staging-windows-v${version_name}+${version_code}.zip"
          windows_installer_basename="phongvu-opshub-staging-windows-setup-v${version_name}+${version_code}"
          windows_installer_name="${windows_installer_basename}.exe"
          windows_checksum_name="phongvu-opshub-staging-windows-v${version_name}+${version_code}.sha256"
          release_notes="Staging GitHub ${GITHUB_SHA::7}"
          echo "version_name=${version_name}" >> "$GITHUB_OUTPUT"
          echo "windows_build_name=${windows_build_name}" >> "$GITHUB_OUTPUT"
          echo "version_code=${version_code}" >> "$GITHUB_OUTPUT"
          echo "apk_name=${apk_name}" >> "$GITHUB_OUTPUT"
          echo "windows_zip_name=${windows_zip_name}" >> "$GITHUB_OUTPUT"
          echo "windows_installer_basename=${windows_installer_basename}" >> "$GITHUB_OUTPUT"
          echo "windows_installer_name=${windows_installer_name}" >> "$GITHUB_OUTPUT"
          echo "windows_checksum_name=${windows_checksum_name}" >> "$GITHUB_OUTPUT"
          echo "release_notes=${release_notes}" >> "$GITHUB_OUTPUT"

  build_android:
    runs-on: ubuntu-latest
    needs: prepare
    environment: staging
    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0

      - name: Set up Flutter
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2
        with:
          channel: stable
          cache: true

      - name: Prepare Android SDK and JDK
        shell: bash
        run: |
          set -euo pipefail
          export JAVA_HOME="${JAVA_HOME_17_X64:-$JAVA_HOME}"
          export PATH="$JAVA_HOME/bin:$PATH"
          java -version
          "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" \
            "platforms;android-36" \
            "build-tools;36.1.0" >/dev/null

      - name: Configure Android staging signing
        shell: bash
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_STAGING_KEYSTORE_BASE64 }}
        run: |
          set -euo pipefail
          if [ -z "$ANDROID_KEYSTORE_BASE64" ]; then
            echo "ANDROID_STAGING_KEYSTORE_BASE64 is required for staging APK signing." >&2
            exit 1
          fi
          printf '%s' "$ANDROID_KEYSTORE_BASE64" | base64 -d > android/app/upload-keystore.jks

      - name: Build staging APK
        shell: bash
        env:
          ANDROID_KEYSTORE_PATH: upload-keystore.jks
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_STAGING_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_STAGING_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_STAGING_KEY_PASSWORD }}
        run: |
          set -euo pipefail
          for key in ANDROID_KEYSTORE_PASSWORD ANDROID_KEY_ALIAS ANDROID_KEY_PASSWORD; do
            if [ -z "${!key}" ]; then
              echo "${key/ANDROID_/ANDROID_STAGING_} is required for staging APK signing." >&2
              exit 1
            fi
          done
          flutter pub get
          flutter build apk --release \
            --flavor staging \
            --build-name "${{ needs.prepare.outputs.version_name }}" \
            --build-number "${{ needs.prepare.outputs.version_code }}" \
            --dart-define "API_BASE_URL=${OPSHUB_API_BASE_URL}" \
            --dart-define "APP_ENV=staging"

      - name: Connect Tailscale
        uses: tailscale/github-action@306e68a486fd2350f2bfc3b19fcd143891a4a2d8
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:ci
          ping: ${{ secrets.OPSHUB_STAGING_VPS_HOST || '100.127.127.89' }}
          version: latest

      - name: Configure SSH
        shell: bash
        env:
          VPS_HOST: ${{ secrets.OPSHUB_STAGING_VPS_HOST || '100.127.127.89' }}
          VPS_PORT: ${{ secrets.OPSHUB_STAGING_VPS_PORT || '22' }}
          VPS_USER: ${{ secrets.OPSHUB_STAGING_VPS_USER || 'hhh' }}
          VPS_SSH_KEY: ${{ secrets.OPSHUB_STAGING_SSH_KEY }}
        run: |
          set -euo pipefail
          if [ -z "$VPS_SSH_KEY" ]; then
            echo "OPSHUB_STAGING_SSH_KEY is required." >&2
            exit 1
          fi
          mkdir -p ~/.ssh
          printf '%s\n' "$VPS_SSH_KEY" > ~/.ssh/opshub_staging_deploy_key
          chmod 600 ~/.ssh/opshub_staging_deploy_key
          ssh-keyscan -p "$VPS_PORT" "$VPS_HOST" >> ~/.ssh/known_hosts
          {
            echo "Host opshub-staging"
            echo "  HostName $VPS_HOST"
            echo "  Port $VPS_PORT"
            echo "  User $VPS_USER"
            echo "  IdentityFile ~/.ssh/opshub_staging_deploy_key"
            echo "  IdentitiesOnly yes"
          } >> ~/.ssh/config

      - name: Upload staging APK
        shell: bash
        run: |
          set -euo pipefail
          staging_dir="${OPSHUB_REMOTE_APP_DIR}/action-staging/${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}/android"
          ssh opshub-staging "mkdir -p '$staging_dir'"
          scp 'build/app/outputs/flutter-apk/app-staging-release.apk' \
            "opshub-staging:${staging_dir}/${{ needs.prepare.outputs.apk_name }}"

  build_windows:
    runs-on: windows-latest
    needs: prepare
    environment: staging
    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0

      - name: Set up Flutter
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2
        with:
          channel: stable
          cache: true

      - name: Apply Windows staging icon
        shell: pwsh
        run: .\scripts\apply-staging-icons.ps1 -Platform windows

      - name: Build Windows staging release
        shell: pwsh
        run: |
          flutter pub get
          flutter build windows --release `
            --build-name "${{ needs.prepare.outputs.windows_build_name }}" `
            --build-number "${{ needs.prepare.outputs.version_code }}" `
            --dart-define "API_BASE_URL=${env:OPSHUB_API_BASE_URL}" `
            --dart-define "APP_ENV=staging"

      - name: Configure Windows staging signing certificate
        shell: pwsh
        env:
          WINDOWS_SIGNING_PFX_BASE64: ${{ secrets.WINDOWS_STAGING_SIGNING_PFX_BASE64 }}
          WINDOWS_SIGNING_PFX_PASSWORD: ${{ secrets.WINDOWS_STAGING_SIGNING_PFX_PASSWORD }}
          WINDOWS_UPDATE_SIGNER_SHA256: ${{ vars.WINDOWS_STAGING_UPDATE_SIGNER_SHA256 }}
        run: |
          if ([string]::IsNullOrWhiteSpace($env:WINDOWS_SIGNING_PFX_BASE64)) {
            throw 'WINDOWS_STAGING_SIGNING_PFX_BASE64 is required; unsigned Windows staging artifacts are forbidden.'
          }
          if ([string]::IsNullOrWhiteSpace($env:WINDOWS_SIGNING_PFX_PASSWORD)) {
            throw 'WINDOWS_STAGING_SIGNING_PFX_PASSWORD is required when WINDOWS_STAGING_SIGNING_PFX_BASE64 is configured.'
          }
          $certPath = Join-Path $env:RUNNER_TEMP 'opshub-staging-windows-signing.pfx'
          [IO.File]::WriteAllBytes($certPath, [Convert]::FromBase64String($env:WINDOWS_SIGNING_PFX_BASE64))
          $cert = [System.Security.Cryptography.X509Certificates.X509Certificate2]::new(
            $certPath,
            $env:WINDOWS_SIGNING_PFX_PASSWORD,
            [System.Security.Cryptography.X509Certificates.X509KeyStorageFlags]::EphemeralKeySet
          )
          try {
            $algorithm = [System.Security.Cryptography.HashAlgorithmName]::SHA256
            $actualPin = $cert.GetCertHashString($algorithm).ToUpperInvariant()
            $trustedPins = @($env:WINDOWS_UPDATE_SIGNER_SHA256 -split '[,;\s]+' | ForEach-Object { $_.ToUpperInvariant() } | Where-Object { $_ })
            if ($actualPin -notin $trustedPins) {
              throw 'Windows staging signing certificate does not match WINDOWS_STAGING_UPDATE_SIGNER_SHA256.'
            }
          } finally {
            $cert.Dispose()
          }
          'WINDOWS_SIGNING_ENABLED=true' | Out-File -FilePath $env:GITHUB_ENV -Append
          "WINDOWS_SIGNING_PFX_PATH=$certPath" | Out-File -FilePath $env:GITHUB_ENV -Append
          "WINDOWS_UPDATE_SIGNER_SHA256=$env:WINDOWS_UPDATE_SIGNER_SHA256" | Out-File -FilePath $env:GITHUB_ENV -Append

      - name: Sign Windows staging executable
        if: env.WINDOWS_SIGNING_ENABLED == 'true'
        shell: pwsh
        env:
          WINDOWS_SIGNING_PFX_PASSWORD: ${{ secrets.WINDOWS_STAGING_SIGNING_PFX_PASSWORD }}
        run: |
          .\scripts\sign-windows-artifact.ps1 -Path 'build\windows\x64\runner\Release\phongvu_opshub.exe'

      - name: Zip Windows staging release
        shell: pwsh
        run: |
          $zipPath = Join-Path "build\windows\x64\runner" "${{ needs.prepare.outputs.windows_zip_name }}"
          Compress-Archive `
            -Path "build\windows\x64\runner\Release\*" `
            -DestinationPath $zipPath `
            -Force

      - name: Install Inno Setup
        shell: pwsh
        run: choco install innosetup --no-progress -y

      - name: Download Visual C++ Redistributable
        shell: pwsh
        run: |
          $prereqDir = New-Item -ItemType Directory -Force -Path "build\windows\prereqs"
          $vcRedistPath = Join-Path $prereqDir.FullName "vc_redist.x64.exe"
          $downloadUrl = "https://aka.ms/vc14/vc_redist.x64.exe"
          $maxAttempts = 5
          $downloaded = $false

          for ($attempt = 1; $attempt -le $maxAttempts; $attempt++) {
            try {
              if (Test-Path $vcRedistPath) {
                Remove-Item -LiteralPath $vcRedistPath -Force
              }
              Invoke-WebRequest `
                -Uri $downloadUrl `
                -OutFile $vcRedistPath `
                -MaximumRetryCount 3 `
                -RetryIntervalSec 2

              $vcRedist = Get-Item $vcRedistPath
              if ($vcRedist.Length -lt 1000000) {
                throw "Downloaded Visual C++ Redistributable is unexpectedly small: $($vcRedist.Length) bytes"
              }

              $signature = Get-AuthenticodeSignature $vcRedistPath
              if ($signature.Status -ne "Valid") {
                throw "Visual C++ Redistributable signature is not valid: $($signature.Status)"
              }

              "VC_REDIST_X64_PATH=$($vcRedist.FullName)" | Out-File -FilePath $env:GITHUB_ENV -Append
              $downloaded = $true
              break
            } catch {
              if ($attempt -eq $maxAttempts) {
                throw
              }
              Write-Warning ("Attempt {0}/{1} to download Visual C++ Redistributable failed: {2}" -f $attempt, $maxAttempts, $_.Exception.Message)
              Start-Sleep -Seconds ([Math]::Min(30, $attempt * 5))
            }
          }

          if (-not $downloaded) {
            throw "Failed to download Visual C++ Redistributable after $maxAttempts attempts."
          }

      - name: Build Windows staging installer
        shell: pwsh
        run: |
          $iscc = "${env:ProgramFiles(x86)}\Inno Setup 6\ISCC.exe"
          if (-not (Test-Path $iscc)) {
            $iscc = "iscc"
          }
          $releaseDir = (Resolve-Path "build\windows\x64\runner\Release").Path
          $outputDir = (Resolve-Path "build\windows\x64\runner").Path
          $isccArgs = @(
            '"' + $iscc + '"',
            '/DAppName="PhongVu OpsHub Staging"',
            '/DAppPublisher="PhongVu OpsHub Staging"',
            '/DAppId="{{6E36D5C9-22C7-48D1-B907-9C8C8C72E77F}"',
            '/DDefaultDirName="{localappdata}\Programs\PhongVu OpsHub Staging"',
            '/DAppVersion="${{ needs.prepare.outputs.version_name }}"',
            '/DSourceDir="' + $releaseDir + '"',
            '/DVcRedistPath="' + $env:VC_REDIST_X64_PATH + '"',
            '/DOutputDir="' + $outputDir + '"',
            '/DOutputBaseFilename="${{ needs.prepare.outputs.windows_installer_basename }}"'
          )
          $isccArgs += '"deploy\windows\phongvu-opshub.iss"'
          $isccCommand = $isccArgs -join ' '
          cmd.exe /c $isccCommand
          if ($LASTEXITCODE -ne 0) {
            throw "Inno Setup failed with exit code $LASTEXITCODE."
          }

      - name: Sign Windows staging installer
        if: env.WINDOWS_SIGNING_ENABLED == 'true'
        shell: pwsh
        env:
          WINDOWS_SIGNING_PFX_PASSWORD: ${{ secrets.WINDOWS_STAGING_SIGNING_PFX_PASSWORD }}
        run: |
          .\scripts\sign-windows-artifact.ps1 -Path 'build\windows\x64\runner\${{ needs.prepare.outputs.windows_installer_name }}'

      - name: Scan final Windows staging artifacts with Microsoft Defender
        shell: pwsh
        run: |
          .\scripts\scan-windows-artifact-defender.ps1 -Path @(
            'build\windows\x64\runner\${{ needs.prepare.outputs.windows_zip_name }}',
            'build\windows\x64\runner\${{ needs.prepare.outputs.windows_installer_name }}'
          )

      - name: Write Windows staging checksums
        shell: pwsh
        run: |
          $runnerDir = 'build\windows\x64\runner'
          $checksumPath = Join-Path $runnerDir '${{ needs.prepare.outputs.windows_checksum_name }}'
          $files = @(
            '${{ needs.prepare.outputs.windows_zip_name }}',
            '${{ needs.prepare.outputs.windows_installer_name }}'
          )
          $lines = foreach ($file in $files) {
            $path = Join-Path $runnerDir $file
            if (-not (Test-Path $path)) {
              throw ('Missing Windows artifact for checksum: {0}' -f $file)
            }
            $hash = (Get-FileHash -Algorithm SHA256 -Path $path).Hash.ToLowerInvariant()
            '{0}  {1}' -f $hash, $file
          }
          Set-Content -Path $checksumPath -Value $lines -Encoding ascii

      - name: Connect Tailscale
        uses: tailscale/github-action@306e68a486fd2350f2bfc3b19fcd143891a4a2d8
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:ci
          ping: ${{ secrets.OPSHUB_STAGING_VPS_HOST || '100.127.127.89' }}
          version: latest

      - name: Configure SSH
        shell: bash
        env:
          VPS_HOST: ${{ secrets.OPSHUB_STAGING_VPS_HOST || '100.127.127.89' }}
          VPS_PORT: ${{ secrets.OPSHUB_STAGING_VPS_PORT || '22' }}
          VPS_USER: ${{ secrets.OPSHUB_STAGING_VPS_USER || 'hhh' }}
          VPS_SSH_KEY: ${{ secrets.OPSHUB_STAGING_SSH_KEY }}
        run: |
          set -euo pipefail
          if [ -z "$VPS_SSH_KEY" ]; then
            echo "OPSHUB_STAGING_SSH_KEY is required." >&2
            exit 1
          fi
          mkdir -p ~/.ssh
          printf '%s\n' "$VPS_SSH_KEY" > ~/.ssh/opshub_staging_deploy_key
          chmod 600 ~/.ssh/opshub_staging_deploy_key
          ssh-keyscan -p "$VPS_PORT" "$VPS_HOST" >> ~/.ssh/known_hosts
          {
            echo "Host opshub-staging"
            echo "  HostName $VPS_HOST"
            echo "  Port $VPS_PORT"
            echo "  User $VPS_USER"
            echo "  IdentityFile ~/.ssh/opshub_staging_deploy_key"
            echo "  IdentitiesOnly yes"
          } >> ~/.ssh/config

      - name: Upload Windows staging artifacts
        shell: bash
        run: |
          set -euo pipefail
          staging_dir="${OPSHUB_REMOTE_APP_DIR}/action-staging/${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}/windows"
          ssh opshub-staging "mkdir -p '$staging_dir'"
          scp \
            'build/windows/x64/runner/${{ needs.prepare.outputs.windows_zip_name }}' \
            'build/windows/x64/runner/${{ needs.prepare.outputs.windows_installer_name }}' \
            'build/windows/x64/runner/${{ needs.prepare.outputs.windows_checksum_name }}' \
            "opshub-staging:${staging_dir}/"

  deploy:
    runs-on: ubuntu-latest
    needs:
      - prepare
      - build_android
      - build_windows
    environment: staging
    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0

      - name: Set up Flutter
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2
        with:
          channel: stable
          cache: true

      - name: Build staging web app
        shell: bash
        run: |
          set -euo pipefail
          pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/apply-staging-icons.ps1 -Platform web
          flutter pub get
          flutter build web --release \
            --no-web-resources-cdn \
            --build-name "${{ needs.prepare.outputs.version_name }}" \
            --build-number "${{ needs.prepare.outputs.version_code }}" \
            --dart-define "API_BASE_URL=${OPSHUB_API_BASE_URL}" \
            --dart-define "APP_ENV=staging"
          WEB_CACHE_BUST_TOKEN="${GITHUB_SHA}" node scripts/patch-flutter-web-cache-busting.mjs

      - name: Connect Tailscale
        uses: tailscale/github-action@306e68a486fd2350f2bfc3b19fcd143891a4a2d8
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:ci
          ping: ${{ secrets.OPSHUB_STAGING_VPS_HOST || '100.127.127.89' }}
          version: latest

      - name: Configure SSH
        shell: bash
        env:
          VPS_HOST: ${{ secrets.OPSHUB_STAGING_VPS_HOST || '100.127.127.89' }}
          VPS_PORT: ${{ secrets.OPSHUB_STAGING_VPS_PORT || '22' }}
          VPS_USER: ${{ secrets.OPSHUB_STAGING_VPS_USER || 'hhh' }}
          VPS_SSH_KEY: ${{ secrets.OPSHUB_STAGING_SSH_KEY }}
        run: |
          set -euo pipefail
          if [ -z "$VPS_SSH_KEY" ]; then
            echo "OPSHUB_STAGING_SSH_KEY is required." >&2
            exit 1
          fi
          mkdir -p ~/.ssh
          printf '%s\n' "$VPS_SSH_KEY" > ~/.ssh/opshub_staging_deploy_key
          chmod 600 ~/.ssh/opshub_staging_deploy_key
          ssh-keyscan -p "$VPS_PORT" "$VPS_HOST" >> ~/.ssh/known_hosts
          {
            echo "Host opshub-staging"
            echo "  HostName $VPS_HOST"
            echo "  Port $VPS_PORT"
            echo "  User $VPS_USER"
            echo "  IdentityFile ~/.ssh/opshub_staging_deploy_key"
            echo "  IdentitiesOnly yes"
          } >> ~/.ssh/config

      - name: Verify staged client artifacts
        shell: bash
        run: |
          set -euo pipefail
          client_staging_dir="${OPSHUB_REMOTE_APP_DIR}/action-staging/${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          ssh opshub-staging \
            "CLIENT_STAGING_DIR='$client_staging_dir' \
             APK_NAME='${{ needs.prepare.outputs.apk_name }}' \
             WINDOWS_ZIP_NAME='${{ needs.prepare.outputs.windows_zip_name }}' \
             WINDOWS_INSTALLER_NAME='${{ needs.prepare.outputs.windows_installer_name }}' \
             WINDOWS_CHECKSUM_NAME='${{ needs.prepare.outputs.windows_checksum_name }}' \
             bash -s" <<'REMOTE'
          set -euo pipefail

          android_artifact="$CLIENT_STAGING_DIR/android/$APK_NAME"
          windows_zip="$CLIENT_STAGING_DIR/windows/$WINDOWS_ZIP_NAME"
          windows_installer="$CLIENT_STAGING_DIR/windows/$WINDOWS_INSTALLER_NAME"
          windows_checksum="$CLIENT_STAGING_DIR/windows/$WINDOWS_CHECKSUM_NAME"

          for file in "$android_artifact" "$windows_zip" "$windows_installer" "$windows_checksum"; do
            if [ ! -s "$file" ]; then
              echo "Missing staged client artifact: $file" >&2
              exit 1
            fi
          done

          REMOTE

      - name: Write staging download manifest
        shell: bash
        env:
          OPSHUB_PUBLIC_BASE_URL: ${{ env.OPSHUB_DOWNLOAD_PUBLIC_BASE_URL }}
          DOWNLOAD_MANIFEST_OUTPUT: dist/downloads/latest.json
          APP_VERSION: ${{ needs.prepare.outputs.version_name }}
          APP_BUILD_NUMBER: ${{ needs.prepare.outputs.version_code }}
          APP_RELEASE_NOTES: ${{ needs.prepare.outputs.release_notes }}
          APK_FILE_NAME: ${{ needs.prepare.outputs.apk_name }}
          WINDOWS_INSTALLER_FILE_NAME: ${{ needs.prepare.outputs.windows_installer_name }}
          WINDOWS_ZIP_FILE_NAME: ${{ needs.prepare.outputs.windows_zip_name }}
          WINDOWS_CHECKSUM_FILE_NAME: ${{ needs.prepare.outputs.windows_checksum_name }}
        run: |
          set -euo pipefail
          client_staging_dir="${OPSHUB_REMOTE_APP_DIR}/action-staging/${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          APK_FILE_SIZE_BYTES="$(ssh opshub-staging "stat -c%s '$client_staging_dir/android/${{ needs.prepare.outputs.apk_name }}'")"
          WINDOWS_INSTALLER_FILE_SIZE_BYTES="$(ssh opshub-staging "stat -c%s '$client_staging_dir/windows/${{ needs.prepare.outputs.windows_installer_name }}'")"
          WINDOWS_ZIP_FILE_SIZE_BYTES="$(ssh opshub-staging "stat -c%s '$client_staging_dir/windows/${{ needs.prepare.outputs.windows_zip_name }}'")"
          WINDOWS_CHECKSUM_FILE_SIZE_BYTES="$(ssh opshub-staging "stat -c%s '$client_staging_dir/windows/${{ needs.prepare.outputs.windows_checksum_name }}'")"
          export APK_FILE_SIZE_BYTES WINDOWS_INSTALLER_FILE_SIZE_BYTES WINDOWS_ZIP_FILE_SIZE_BYTES WINDOWS_CHECKSUM_FILE_SIZE_BYTES
          node scripts/download-manifest.mjs from-published

      - name: Stage app bundle and download metadata
        shell: bash
        run: |
          set -euo pipefail
          node scripts/build-runtime-release.mjs
          tar -C docs/help -czf dist/help-assets.tar.gz assets
          tar -C build/web -czf dist/web.tar.gz .
          remote_release_dir="${OPSHUB_REMOTE_APP_DIR}/releases/${GITHUB_SHA}-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          tmp_dir="${OPSHUB_REMOTE_APP_DIR}/tmp/${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          ssh opshub-staging "mkdir -p '$remote_release_dir' '$tmp_dir'"
          rsync -az --delete 'dist/runtime-release/' \
            "opshub-staging:${remote_release_dir}/"
          scp 'dist/downloads/latest.json' "opshub-staging:${tmp_dir}/latest.json"
          scp 'deploy/home-server/download.html' "opshub-staging:${tmp_dir}/download.html"
          scp 'web/icons/Icon-192.png' "opshub-staging:${tmp_dir}/opshub-icon-192.png"
          scp 'dist/help-assets.tar.gz' "opshub-staging:${tmp_dir}/help-assets.tar.gz"
          scp 'dist/web.tar.gz' "opshub-staging:${tmp_dir}/web.tar.gz"

      - name: Deploy staging backend and publish version metadata
        id: deploy_runtime
        shell: bash
        run: |
          set -euo pipefail
          remote_release_dir="${OPSHUB_REMOTE_APP_DIR}/releases/${GITHUB_SHA}-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          client_staging_dir="${OPSHUB_REMOTE_APP_DIR}/action-staging/${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          android_update_url="${OPSHUB_DOWNLOAD_PUBLIC_BASE_URL}/downloads/${{ needs.prepare.outputs.apk_name }}"
          windows_update_url="${OPSHUB_DOWNLOAD_PUBLIC_BASE_URL}/downloads/${{ needs.prepare.outputs.windows_installer_name }}"
          downloads_dir="${OPSHUB_SSD_ROOT}/downloads"
          web_dir="${OPSHUB_SSD_ROOT}/web"
          tmp_dir="${OPSHUB_REMOTE_APP_DIR}/tmp/${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          android_package_sha256="$(ssh opshub-staging "sha256sum '$client_staging_dir/android/${{ needs.prepare.outputs.apk_name }}' | awk '{print \$1}'")"
          windows_package_sha256="$(ssh opshub-staging "sha256sum '$client_staging_dir/windows/${{ needs.prepare.outputs.windows_installer_name }}' | awk '{print \$1}'")"
          android_package_size_bytes="$(ssh opshub-staging "stat -c%s '$client_staging_dir/android/${{ needs.prepare.outputs.apk_name }}'")"
          windows_package_size_bytes="$(ssh opshub-staging "stat -c%s '$client_staging_dir/windows/${{ needs.prepare.outputs.windows_installer_name }}'")"
          windows_installer_args="/VERYSILENT,/SUPPRESSMSGBOXES,/NORESTART,/CLOSEAPPLICATIONS,/OPSHUBRELAUNCH=1"
          ssh opshub-staging \
            "REMOTE_RELEASE_DIR='$remote_release_dir' \
             CLIENT_STAGING_DIR='$client_staging_dir' \
             TMP_DIR='$tmp_dir' \
             DOWNLOADS_DIR='$downloads_dir' \
             WEB_DIR='$web_dir' \
             APK_NAME='${{ needs.prepare.outputs.apk_name }}' \
             WINDOWS_ZIP_NAME='${{ needs.prepare.outputs.windows_zip_name }}' \
             WINDOWS_INSTALLER_NAME='${{ needs.prepare.outputs.windows_installer_name }}' \
             WINDOWS_CHECKSUM_NAME='${{ needs.prepare.outputs.windows_checksum_name }}' \
             OPSHUB_REMOTE_APP_DIR='${OPSHUB_REMOTE_APP_DIR}' \
             CURRENT_DIR='${OPSHUB_REMOTE_APP_DIR}/current' \
             DEPLOY_RUN_ID='$GITHUB_RUN_ID' \
             OPSHUB_ENV_FILE='$OPSHUB_ENV_FILE' \
             OPSHUB_SSD_ROOT='$OPSHUB_SSD_ROOT' \
             COMPOSE_PROJECT_NAME='$COMPOSE_PROJECT_NAME' \
             OPSHUB_PUBLIC_BASE_URL='$OPSHUB_PUBLIC_BASE_URL' \
             APP_VERSION='${{ needs.prepare.outputs.version_name }}' \
             APP_BUILD_NUMBER='${{ needs.prepare.outputs.version_code }}' \
             ANDROID_UPDATE_URL='$android_update_url' \
             WINDOWS_UPDATE_URL='$windows_update_url' \
             ANDROID_PACKAGE_SHA256='$android_package_sha256' \
             WINDOWS_PACKAGE_SHA256='$windows_package_sha256' \
             ANDROID_PACKAGE_SIZE_BYTES='$android_package_size_bytes' \
             WINDOWS_PACKAGE_SIZE_BYTES='$windows_package_size_bytes' \
             WINDOWS_INSTALLER_ARGS='$windows_installer_args' \
             APP_RELEASE_NOTES='${{ needs.prepare.outputs.release_notes }}' \
             bash -s" <<'REMOTE'
          set -euo pipefail

          if [ ! -s "$OPSHUB_ENV_FILE" ]; then
            echo "Missing staging env file: $OPSHUB_ENV_FILE" >&2
            echo "Run bash deploy/staging/bootstrap.sh on mementoamoris and replace placeholders first." >&2
            exit 1
          fi

          case "$DEPLOY_RUN_ID" in
            ''|*[!0-9]*)
              echo "Invalid staging deploy run id." >&2
              exit 1
              ;;
          esac

          previous_current="$(readlink -f "$CURRENT_DIR" || true)"
          remote_release_real="$(readlink -f "$REMOTE_RELEASE_DIR" || true)"
          releases_dir="$(readlink -f "$(dirname "$REMOTE_RELEASE_DIR")" || true)"
          if [ -z "$previous_current" ] || [ ! -d "$previous_current" ]; then
            echo "A healthy previous staging release is required before deploy." >&2
            exit 1
          fi
          case "$previous_current" in
            "$releases_dir"/*) ;;
            *)
              echo "Previous staging release is outside the protected releases directory." >&2
              exit 1
              ;;
          esac
          if [ -z "$remote_release_real" ]; then
            echo "The uploaded staging release is unavailable." >&2
            exit 1
          fi

          if [ "$DOWNLOADS_DIR" != "${OPSHUB_SSD_ROOT}/downloads" ] || \
             [ "$WEB_DIR" != "${OPSHUB_SSD_ROOT}/web" ]; then
            echo "Shared staging publication paths are outside OPSHUB_SSD_ROOT." >&2
            exit 1
          fi
          case "$CLIENT_STAGING_DIR" in
            "$OPSHUB_REMOTE_APP_DIR"/action-staging/*) ;;
            *) echo "Invalid staged client directory." >&2; exit 1 ;;
          esac
          case "$TMP_DIR" in
            "$OPSHUB_REMOTE_APP_DIR"/tmp/*) ;;
            *) echo "Invalid staged static directory." >&2; exit 1 ;;
          esac
          for artifact_name in "$APK_NAME" "$WINDOWS_ZIP_NAME" "$WINDOWS_INSTALLER_NAME" "$WINDOWS_CHECKSUM_NAME"; do
            if [ -z "$artifact_name" ] || [ "$(basename -- "$artifact_name")" != "$artifact_name" ]; then
              echo "Invalid staged client artifact name." >&2
              exit 1
            fi
          done

          android_artifact="$CLIENT_STAGING_DIR/android/$APK_NAME"
          windows_zip="$CLIENT_STAGING_DIR/windows/$WINDOWS_ZIP_NAME"
          windows_installer="$CLIENT_STAGING_DIR/windows/$WINDOWS_INSTALLER_NAME"
          windows_checksum="$CLIENT_STAGING_DIR/windows/$WINDOWS_CHECKSUM_NAME"
          for staged_file in \
            "$android_artifact" \
            "$windows_zip" \
            "$windows_installer" \
            "$windows_checksum" \
            "$TMP_DIR/latest.json" \
            "$TMP_DIR/download.html" \
            "$TMP_DIR/opshub-icon-192.png" \
            "$TMP_DIR/help-assets.tar.gz" \
            "$TMP_DIR/web.tar.gz"; do
            if [ ! -s "$staged_file" ]; then
              echo "Missing staged release input: $staged_file" >&2
              exit 1
            fi
          done

          env_snapshot="${OPSHUB_ENV_FILE}.rollback.${DEPLOY_RUN_ID}"
          rollback_state_dir="${OPSHUB_SSD_ROOT}/rollback"
          rollback_state="${rollback_state_dir}/deploy-${DEPLOY_RUN_ID}.state"
          rollback_state_tmp="${rollback_state}.tmp"
          shared_snapshot_dir="${rollback_state_dir}/deploy-${DEPLOY_RUN_ID}.shared"
          shared_stage_dir="${rollback_state_dir}/deploy-${DEPLOY_RUN_ID}.stage"
          if sudo test -e "$env_snapshot"; then
            echo "Refusing to overwrite an existing staging env rollback snapshot." >&2
            exit 1
          fi
          if sudo test -e "$rollback_state" || \
             sudo test -e "$rollback_state_tmp" || \
             sudo test -e "$shared_snapshot_dir" || \
             sudo test -e "$shared_stage_dir"; then
            echo "Refusing to overwrite existing staging rollback metadata." >&2
            exit 1
          fi
          sudo cp --preserve=mode,ownership,timestamps -- "$OPSHUB_ENV_FILE" "$env_snapshot"

          restore_env_snapshot() {
            local restore_tmp
            if [ -z "${env_snapshot:-}" ] || ! sudo test -s "$env_snapshot"; then
              echo "Staging env rollback snapshot is unavailable; refusing a partial rollback." >&2
              return 1
            fi
            restore_tmp="${OPSHUB_ENV_FILE}.restore.${DEPLOY_RUN_ID}"
            sudo cp --preserve=mode,ownership,timestamps -- "$env_snapshot" "$restore_tmp" || return 1
            sudo mv -Tf -- "$restore_tmp" "$OPSHUB_ENV_FILE" || return 1
          }

          rollback_shared_publication() {
            local backup_path live_path name
            if ! sudo test -e "$shared_snapshot_dir" && ! sudo test -e "$shared_stage_dir"; then
              return 0
            fi

            if sudo test -e "$shared_snapshot_dir/PROMOTION_STARTED"; then
              if ! sudo test -e "$shared_snapshot_dir/SNAPSHOT_READY"; then
                echo "Shared staging publication started without a complete snapshot; refusing partial restore." >&2
                return 1
              fi

              sudo rm -rf -- "$WEB_DIR" || return 1
              if sudo test -e "$shared_snapshot_dir/web"; then
                sudo cp -a --reflink=auto -- "$shared_snapshot_dir/web" "$WEB_DIR" || return 1
              fi

              sudo rm -rf -- "$DOWNLOADS_DIR/help" || return 1
              if sudo test -e "$shared_snapshot_dir/downloads/help"; then
                sudo cp -a --reflink=auto -- "$shared_snapshot_dir/downloads/help" "$DOWNLOADS_DIR/help" || return 1
              fi

              for name in latest.json download.html opshub-icon-192.png; do
                live_path="$DOWNLOADS_DIR/$name"
                backup_path="$shared_snapshot_dir/downloads/$name"
                sudo rm -f -- "$live_path" || return 1
                if sudo test -e "$backup_path"; then
                  sudo cp -a --reflink=auto -- "$backup_path" "$live_path" || return 1
                fi
              done

              for name in "$APK_NAME" "$WINDOWS_ZIP_NAME" "$WINDOWS_INSTALLER_NAME" "$WINDOWS_CHECKSUM_NAME"; do
                live_path="$DOWNLOADS_DIR/$name"
                backup_path="$shared_snapshot_dir/versioned/$name"
                sudo rm -f -- "$live_path" || return 1
                if sudo test -e "$backup_path"; then
                  sudo cp -a --reflink=auto -- "$backup_path" "$live_path" || return 1
                fi
              done
              if ! sudo test -e "$shared_snapshot_dir/DOWNLOADS_DIR_PRESENT"; then
                sudo rmdir -- "$DOWNLOADS_DIR" || return 1
              fi
            fi
          }

          cleanup_rollback_metadata() {
            sudo rm -f -- "$rollback_state_tmp" "$rollback_state" "$env_snapshot"
            sudo rm -rf -- "$shared_stage_dir" "$shared_snapshot_dir"
            env_snapshot=""
          }

          rollback_env_before_runtime_switch() {
            local status="$1"
            local env_restored=false
            local shared_restored=false
            trap - ERR
            echo "Staging publication failed before runtime switch; restoring protected env and shared state." >&2
            if restore_env_snapshot; then
              env_restored=true
            else
              echo "Staging env restore failed before runtime switch; manual recovery is required." >&2
            fi
            if rollback_shared_publication; then
              shared_restored=true
            else
              echo "Shared staging publication restore failed before runtime switch; manual recovery is required." >&2
            fi
            if [ "$env_restored" = true ] && [ "$shared_restored" = true ]; then
              cleanup_rollback_metadata
            else
              echo "Rollback evidence was retained under $rollback_state_dir." >&2
            fi
            exit "$status"
          }

          trap 'rollback_env_before_runtime_switch $?' ERR

          sudo install -d -m 0700 "$rollback_state_dir"
          printf '%s\n%s\n' "$previous_current" "$remote_release_real" |
            sudo tee "$rollback_state_tmp" >/dev/null
          sudo chmod 0600 "$rollback_state_tmp"
          sudo mv -Tf -- "$rollback_state_tmp" "$rollback_state"

          snapshot_and_promote_shared_publication() {
            local backup_path live_path name staged_path
            sudo install -d -m 0700 \
              "$shared_snapshot_dir" \
              "$shared_snapshot_dir/downloads" \
              "$shared_snapshot_dir/versioned" \
              "$shared_stage_dir" \
              "$shared_stage_dir/downloads"
            sudo install -d -m 0755 "$shared_stage_dir/web" "$shared_stage_dir/help"

            sudo tar --no-same-owner -xzf "$TMP_DIR/web.tar.gz" -C "$shared_stage_dir/web"
            sudo tar --no-same-owner -xzf "$TMP_DIR/help-assets.tar.gz" -C "$shared_stage_dir/help"
            if ! sudo test -s "$shared_stage_dir/web/index.html" || \
               ! sudo test -d "$shared_stage_dir/help/assets"; then
              echo "Staged web or Help bundle is incomplete." >&2
              return 1
            fi

            sudo install -m 0644 "$TMP_DIR/latest.json" "$shared_stage_dir/downloads/latest.json"
            sudo install -m 0644 "$TMP_DIR/download.html" "$shared_stage_dir/downloads/download.html"
            sudo install -m 0644 "$TMP_DIR/opshub-icon-192.png" "$shared_stage_dir/downloads/opshub-icon-192.png"
            sudo install -m 0644 "$android_artifact" "$shared_stage_dir/downloads/$APK_NAME"
            sudo install -m 0644 "$windows_zip" "$shared_stage_dir/downloads/$WINDOWS_ZIP_NAME"
            sudo install -m 0644 "$windows_installer" "$shared_stage_dir/downloads/$WINDOWS_INSTALLER_NAME"
            sudo install -m 0644 "$windows_checksum" "$shared_stage_dir/downloads/$WINDOWS_CHECKSUM_NAME"

            if sudo test -e "$DOWNLOADS_DIR"; then
              if ! sudo test -d "$DOWNLOADS_DIR" || sudo test -L "$DOWNLOADS_DIR"; then
                echo "Existing staging downloads path is not a real directory." >&2
                return 1
              fi
              sudo install -m 0600 /dev/null "$shared_snapshot_dir/DOWNLOADS_DIR_PRESENT"
            fi
            if sudo test -e "$WEB_DIR"; then
              if ! sudo test -d "$WEB_DIR" || sudo test -L "$WEB_DIR"; then
                echo "Existing staging web path is not a real directory." >&2
                return 1
              fi
              sudo cp -a --reflink=auto -- "$WEB_DIR" "$shared_snapshot_dir/web"
            fi
            if sudo test -e "$DOWNLOADS_DIR/help"; then
              if ! sudo test -d "$DOWNLOADS_DIR/help" || sudo test -L "$DOWNLOADS_DIR/help"; then
                echo "Existing staging Help path is not a real directory." >&2
                return 1
              fi
              sudo cp -a --reflink=auto -- "$DOWNLOADS_DIR/help" "$shared_snapshot_dir/downloads/help"
            fi

            for name in latest.json download.html opshub-icon-192.png; do
              live_path="$DOWNLOADS_DIR/$name"
              backup_path="$shared_snapshot_dir/downloads/$name"
              if sudo test -e "$live_path"; then
                if ! sudo test -f "$live_path" || sudo test -L "$live_path"; then
                  echo "Existing staging shared file has an unexpected type: $live_path" >&2
                  return 1
                fi
                sudo cp -a --reflink=auto -- "$live_path" "$backup_path"
              fi
            done
            for name in "$APK_NAME" "$WINDOWS_ZIP_NAME" "$WINDOWS_INSTALLER_NAME" "$WINDOWS_CHECKSUM_NAME"; do
              live_path="$DOWNLOADS_DIR/$name"
              backup_path="$shared_snapshot_dir/versioned/$name"
              if sudo test -e "$live_path"; then
                if ! sudo test -f "$live_path" || sudo test -L "$live_path"; then
                  echo "Existing staging versioned file has an unexpected type: $live_path" >&2
                  return 1
                fi
                sudo cp -a --reflink=auto -- "$live_path" "$backup_path"
              fi
            done

            sudo install -m 0600 /dev/null "$shared_snapshot_dir/SNAPSHOT_READY"
            sudo install -m 0600 /dev/null "$shared_snapshot_dir/PROMOTION_STARTED"
            if ! sudo test -e "$shared_snapshot_dir/DOWNLOADS_DIR_PRESENT"; then
              sudo install -d -m 0755 "$DOWNLOADS_DIR"
            fi

            sudo rm -rf -- "$WEB_DIR"
            sudo mv -T -- "$shared_stage_dir/web" "$WEB_DIR"
            sudo rm -rf -- "$DOWNLOADS_DIR/help"
            sudo mv -T -- "$shared_stage_dir/help" "$DOWNLOADS_DIR/help"

            for name in latest.json download.html opshub-icon-192.png; do
              staged_path="$shared_stage_dir/downloads/$name"
              sudo mv -Tf -- "$staged_path" "$DOWNLOADS_DIR/$name"
            done
            for name in "$APK_NAME" "$WINDOWS_ZIP_NAME" "$WINDOWS_INSTALLER_NAME" "$WINDOWS_CHECKSUM_NAME"; do
              staged_path="$shared_stage_dir/downloads/$name"
              sudo mv -Tf -- "$staged_path" "$DOWNLOADS_DIR/$name"
            done
            sudo install -m 0600 /dev/null "$shared_snapshot_dir/PROMOTED"
          }

          upsert_env() {
            key="$1"
            value="$2"
            escaped_value=$(printf '%s' "$value" | sed 's/[\/&]/\\&/g')
            if sudo grep -q "^${key}=" "$OPSHUB_ENV_FILE"; then
              sudo sed -i "s/^${key}=.*/${key}=${escaped_value}/" "$OPSHUB_ENV_FILE"
            else
              printf '%s=%s\n' "$key" "$value" | sudo tee -a "$OPSHUB_ENV_FILE" >/dev/null
            fi
          }

          unset_env() {
            key="$1"
            sudo sed -i "/^${key}=/d" "$OPSHUB_ENV_FILE"
          }

          upsert_env OPSHUB_STAGING true
          upsert_env COMPOSE_PROJECT_NAME "$COMPOSE_PROJECT_NAME"
          upsert_env OPSHUB_DOMAIN "opshub-staging.hoanghochoi.com"
          upsert_env PUBLIC_BASE_URL "$OPSHUB_PUBLIC_BASE_URL"
          upsert_env OPSHUB_SSD_ROOT "$OPSHUB_SSD_ROOT"
          upsert_env OPSHUB_HTTP_BIND "127.0.0.1:8090"
          upsert_env OPSHUB_HTTPS_BIND "127.0.0.1:8443"
          upsert_env IMAGE_BASE_URL "${OPSHUB_PUBLIC_BASE_URL}/uploads"
          upsert_env ALLOWED_ORIGINS "$OPSHUB_PUBLIC_BASE_URL"
          upsert_env OPSHUB_STAGING_LOAD_MAINTENANCE_ENABLED true
          upsert_env OPSHUB_STAGING_LOAD_OUTPUT_DIR /srv/opshub-staging/load-output
          upsert_env ERP_ORDER_CACHE_SYNC_ENABLED false
          upsert_env ERP_ORDER_STATUS_SYNC_ENABLED false
          upsert_env VIETQR_AUTO_RECONCILE_ENABLED false
          upsert_env MAP_VIETIN_GLOBAL_SYNC_ENABLED false
          upsert_env HOME_SUMMARY_ERP_BACKFILL_ENABLED false
          for smtp_key in SMTP_HOST SMTP_PORT SMTP_SECURE SMTP_USER SMTP_PASS SMTP_FROM; do
            unset_env "$smtp_key"
          done

          upsert_env APP_VERSION "$APP_VERSION"
          upsert_env APP_BUILD_NUMBER "$APP_BUILD_NUMBER"
          upsert_env APP_MIN_SUPPORTED_BUILD "$APP_BUILD_NUMBER"
          upsert_env APP_UPDATE_URL "$ANDROID_UPDATE_URL"
          upsert_env APP_PACKAGE_URL "$ANDROID_UPDATE_URL"
          upsert_env APP_PACKAGE_SHA256 "$ANDROID_PACKAGE_SHA256"
          upsert_env APP_PACKAGE_SIZE_BYTES "$ANDROID_PACKAGE_SIZE_BYTES"
          upsert_env APP_PACKAGE_TYPE apk
          upsert_env APP_RELEASE_NOTES "$APP_RELEASE_NOTES"
          upsert_env APP_FORCE_UPDATE true

          upsert_env APP_ANDROID_APP_VERSION "$APP_VERSION"
          upsert_env APP_ANDROID_APP_BUILD_NUMBER "$APP_BUILD_NUMBER"
          upsert_env APP_ANDROID_APP_MIN_SUPPORTED_BUILD "$APP_BUILD_NUMBER"
          upsert_env APP_ANDROID_APP_UPDATE_URL "$ANDROID_UPDATE_URL"
          upsert_env APP_ANDROID_APP_PACKAGE_URL "$ANDROID_UPDATE_URL"
          upsert_env APP_ANDROID_APP_PACKAGE_SHA256 "$ANDROID_PACKAGE_SHA256"
          upsert_env APP_ANDROID_APP_PACKAGE_SIZE_BYTES "$ANDROID_PACKAGE_SIZE_BYTES"
          upsert_env APP_ANDROID_APP_PACKAGE_TYPE apk
          upsert_env APP_ANDROID_APP_RELEASE_NOTES "$APP_RELEASE_NOTES"
          upsert_env APP_ANDROID_APP_FORCE_UPDATE true

          upsert_env APP_WINDOWS_APP_VERSION "$APP_VERSION"
          upsert_env APP_WINDOWS_APP_BUILD_NUMBER "$APP_BUILD_NUMBER"
          upsert_env APP_WINDOWS_APP_MIN_SUPPORTED_BUILD "$APP_BUILD_NUMBER"
          upsert_env APP_WINDOWS_APP_UPDATE_URL "$WINDOWS_UPDATE_URL"
          upsert_env APP_WINDOWS_APP_PACKAGE_URL "$WINDOWS_UPDATE_URL"
          upsert_env APP_WINDOWS_APP_PACKAGE_SHA256 "$WINDOWS_PACKAGE_SHA256"
          upsert_env APP_WINDOWS_APP_PACKAGE_SIZE_BYTES "$WINDOWS_PACKAGE_SIZE_BYTES"
          upsert_env APP_WINDOWS_APP_PACKAGE_TYPE windowsInstaller
          upsert_env APP_WINDOWS_APP_INSTALLER_ARGS "$WINDOWS_INSTALLER_ARGS"
          upsert_env APP_WINDOWS_APP_RELEASE_NOTES "$APP_RELEASE_NOTES"
          upsert_env APP_WINDOWS_APP_FORCE_UPDATE true

          upsert_env APP_WEB_APP_VERSION "$APP_VERSION"
          upsert_env APP_WEB_APP_BUILD_NUMBER "$APP_BUILD_NUMBER"
          upsert_env APP_WEB_APP_MIN_SUPPORTED_BUILD 1
          upsert_env APP_WEB_APP_UPDATE_URL ""
          upsert_env APP_WEB_APP_RELEASE_NOTES "$APP_RELEASE_NOTES"
          upsert_env APP_WEB_APP_FORCE_UPDATE false

          docker run --rm \
            --env-file "$OPSHUB_ENV_FILE" \
            -v "$REMOTE_RELEASE_DIR/deploy/home-server/Caddyfile:/etc/caddy/Caddyfile:ro" \
            caddy:2-alpine caddy validate --config /etc/caddy/Caddyfile < /dev/null

          snapshot_and_promote_shared_publication
          cd "$REMOTE_RELEASE_DIR"
          export COMPOSE_PROJECT_NAME OPSHUB_ENV_FILE
          compose=(docker compose --env-file "$OPSHUB_ENV_FILE" -f deploy/home-server/docker-compose.home.yml)
          compose_cmd() {
            "${compose[@]}" "$@" < /dev/null
          }

          dump_deploy_diagnostics() {
            set +e
            echo "::group::Staging Docker deployment diagnostics"
            compose_cmd ps
            compose_cmd logs --tail=200 api
            compose_cmd logs --tail=120 realtime
            compose_cmd logs --tail=120 caddy
            echo "::endgroup::"
            set -e
          }

          rollback_to_release() {
            candidate="$1"
            if [ ! -d "$candidate" ]; then
              return 1
            fi
            echo "Attempting staging rollback to release: $candidate"
            ln -sfn "$candidate" "${CURRENT_DIR}.rollback"
            mv -Tf "${CURRENT_DIR}.rollback" "$CURRENT_DIR"
            cd "$CURRENT_DIR"
            compose=(docker compose --env-file "$OPSHUB_ENV_FILE" -f deploy/home-server/docker-compose.home.yml)
            if compose_cmd up -d --build --force-recreate --wait --wait-timeout 240 redis api realtime caddy; then
              echo "Staging rollback release is healthy: $candidate"
              compose_cmd ps
              return 0
            fi
            dump_deploy_diagnostics
            return 1
          }

          rollback_on_error() {
            local status="$1"
            local env_restored=false
            local shared_restored=false
            trap - ERR
            echo "Staging deploy failed with status $status" >&2
            dump_deploy_diagnostics

            if restore_env_snapshot; then
              env_restored=true
            else
              echo "Staging runtime env restore failed; rollback evidence was retained." >&2
            fi
            if rollback_shared_publication; then
              shared_restored=true
            else
              echo "Staging shared publication restore failed; rollback evidence was retained." >&2
            fi
            if [ "$env_restored" != true ]; then
              echo "Staging runtime rollback cannot recreate the prior release without its protected env." >&2
              exit "$status"
            fi

            rollback_candidates=()
            if [ -n "${previous_current:-}" ] && [ -d "$previous_current" ]; then
              rollback_candidates+=("$previous_current")
            fi

            releases_dir="$(dirname "$REMOTE_RELEASE_DIR")"
            while IFS= read -r dir; do
              dir_real="$(readlink -f "$dir" || true)"
              if [ -z "$dir_real" ] || [ "$dir_real" = "$remote_release_real" ]; then
                continue
              fi
              already_added=false
              for existing in "${rollback_candidates[@]}"; do
                existing_real="$(readlink -f "$existing" || true)"
                if [ "$existing_real" = "$dir_real" ]; then
                  already_added=true
                  break
                fi
              done
              if [ "$already_added" = false ]; then
                rollback_candidates+=("$dir_real")
              fi
            done < <(find "$releases_dir" -mindepth 1 -maxdepth 1 -type d -printf '%T@ %p\n' | sort -rn | awk '{print $2}')

            for candidate in "${rollback_candidates[@]}"; do
              if rollback_to_release "$candidate"; then
                if [ "$shared_restored" = true ]; then
                  cleanup_rollback_metadata
                else
                  echo "Prior runtime is healthy but shared-state rollback needs manual recovery." >&2
                fi
                exit "$status"
              fi
            done

            echo "Staging rollback did not find a healthy release" >&2
            exit "$status"
          }

          trap 'rollback_on_error $?' ERR
          ln -sfn "$REMOTE_RELEASE_DIR" "${CURRENT_DIR}.next"
          mv -Tf "${CURRENT_DIR}.next" "$CURRENT_DIR"
          cd "$CURRENT_DIR"
          compose_cmd config >/dev/null

          resolve_failed_node_feature_migration() {
            local migration_name="20260613190000_node_feature_assignments"
            local failed_count
            local table_exists

            compose_cmd up -d --no-recreate --wait postgres >/dev/null

            failed_count="$(
              compose_cmd \
                exec -T postgres sh -lc \
                'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -tAc "SELECT COUNT(*) FROM _prisma_migrations WHERE migration_name = '\''20260613190000_node_feature_assignments'\'' AND finished_at IS NULL AND rolled_back_at IS NULL;"'
            )"
            failed_count="$(printf '%s' "$failed_count" | tr -d '[:space:]')"
            if [ "${failed_count:-0}" = "0" ]; then
              return 0
            fi

            table_exists="$(
              compose_cmd \
                exec -T postgres sh -lc \
                'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -tAc "SELECT to_regclass('\''public.\"OrganizationNodeFeatureAssignment\"'\'') IS NOT NULL;"'
            )"
            table_exists="$(printf '%s' "$table_exists" | tr -d '[:space:]')"
            if [ "$table_exists" != "f" ]; then
              echo "Refusing to auto-resolve $migration_name because OrganizationNodeFeatureAssignment already exists." >&2
              return 1
            fi

            echo "Resolving stale failed migration state for $migration_name before rerun."
            compose_cmd \
              --profile migrate run --rm -T --build migrate \
              npx prisma migrate resolve --rolled-back "$migration_name"
          }

          resolve_failed_quick_actions_migration() {
            local migration_name="20260713210000_quick_actions_v1"
            local failed_count
            local link_count

            compose_cmd up -d --no-recreate --wait postgres >/dev/null

            failed_count="$(
              compose_cmd \
                exec -T postgres sh -lc \
                'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -tAc "SELECT COUNT(*) FROM _prisma_migrations WHERE migration_name = '\''20260713210000_quick_actions_v1'\'' AND finished_at IS NULL AND rolled_back_at IS NULL;"'
            )"
            failed_count="$(printf '%s' "$failed_count" | tr -d '[:space:]')"
            if [ "${failed_count:-0}" = "0" ]; then
              return 0
            fi
            if [ "$failed_count" != "1" ]; then
              echo "Refusing to auto-resolve $migration_name because failed row count is $failed_count." >&2
              return 1
            fi

            link_count="$(
              compose_cmd \
                exec -T postgres sh -lc \
                'psql -v ON_ERROR_STOP=1 -U "$POSTGRES_USER" -d "$POSTGRES_DB" -tAc "SELECT COUNT(*) FROM \"QuickActionLink\";"'
            )"
            link_count="$(printf '%s' "$link_count" | tr -d '[:space:]')"
            if [ "${link_count:-0}" != "0" ]; then
              echo "Refusing to auto-resolve $migration_name because QuickActionLink contains $link_count rows." >&2
              return 1
            fi

            echo "Resetting the empty partial QuickActionLink table for failed migration $migration_name."
            compose_cmd \
              exec -T postgres sh -lc \
              'psql -v ON_ERROR_STOP=1 -U "$POSTGRES_USER" -d "$POSTGRES_DB" -c '\''DROP TABLE IF EXISTS "QuickActionLink";'\'''
            compose_cmd \
              --profile migrate run --rm -T --build migrate \
              npx prisma migrate resolve --rolled-back "$migration_name"
          }

          resolve_failed_node_feature_migration
          resolve_failed_quick_actions_migration

          compose_cmd --profile migrate run --rm -T --build migrate

          compose_cmd stop api realtime caddy || true
          compose_cmd rm -f api realtime caddy || true

          compose_cmd up -d --build --force-recreate --wait --wait-timeout 240 api realtime caddy

          expected_app_env="${APP_VERSION}|${APP_BUILD_NUMBER}|${APP_VERSION}|${APP_BUILD_NUMBER}|${APP_VERSION}|${APP_BUILD_NUMBER}|1|false"
          expected_side_effect_env='false|false|false|false|false||||||'
          for attempt in {1..12}; do
            current_app_env="$(
              compose_cmd \
                exec -T api sh -lc \
                'printf "%s|%s|%s|%s|%s|%s|%s|%s" "$APP_ANDROID_APP_VERSION" "$APP_ANDROID_APP_BUILD_NUMBER" "$APP_WINDOWS_APP_VERSION" "$APP_WINDOWS_APP_BUILD_NUMBER" "$APP_WEB_APP_VERSION" "$APP_WEB_APP_BUILD_NUMBER" "$APP_WEB_APP_MIN_SUPPORTED_BUILD" "$APP_WEB_APP_FORCE_UPDATE"' \
                2>/dev/null || true
            )"
            current_side_effect_env="$(
              compose_cmd \
                exec -T api sh -lc \
                'printf "%s|%s|%s|%s|%s|%s|%s|%s|%s|%s|%s" "$ERP_ORDER_CACHE_SYNC_ENABLED" "$ERP_ORDER_STATUS_SYNC_ENABLED" "$VIETQR_AUTO_RECONCILE_ENABLED" "$MAP_VIETIN_GLOBAL_SYNC_ENABLED" "$HOME_SUMMARY_ERP_BACKFILL_ENABLED" "${SMTP_HOST:+configured}" "${SMTP_PORT:+configured}" "${SMTP_SECURE:+configured}" "${SMTP_USER:+configured}" "${SMTP_PASS:+configured}" "${SMTP_FROM:+configured}"' \
                2>/dev/null || true
            )"
            if [ "$current_app_env" = "$expected_app_env" ] && \
               [ "$current_side_effect_env" = "$expected_side_effect_env" ]; then
              break
            fi
            if [ "$attempt" -eq 12 ]; then
              echo "API container env mismatch after recreate: appVersionMatch=$([ "$current_app_env" = "$expected_app_env" ] && printf true || printf false) sideEffectIsolationMatch=$([ "$current_side_effect_env" = "$expected_side_effect_env" ] && printf true || printf false)" >&2
              false
            fi
            sleep 5
          done

          verify_direct_origin_route() {
            local slash_path="$1"
            local canonical_path="$2"
            local origin='http://127.0.0.1:8090'
            local host='opshub-staging.hoanghochoi.com'
            local slash_headers canonical_headers canonical_body
            local slash_status canonical_status location
            slash_headers="$(mktemp)"
            canonical_headers="$(mktemp)"
            canonical_body="$(mktemp)"

            if ! slash_status="$(
              curl -sS -D "$slash_headers" -o /dev/null -w '%{http_code}' \
                -H "Host: $host" -H 'X-Forwarded-Proto: https' \
                "${origin}${slash_path}"
            )"; then
              rm -f "$slash_headers" "$canonical_headers" "$canonical_body"
              echo "Direct origin ${slash_path} did not return an HTTP response." >&2
              return 1
            fi
            if [ "$slash_status" != '308' ]; then
              rm -f "$slash_headers" "$canonical_headers" "$canonical_body"
              echo "Direct origin ${slash_path} returned ${slash_status}; expected 308." >&2
              return 1
            fi
            location="$(
              awk 'tolower($1) == "location:" { $1=""; sub(/^[[:space:]]+/, ""); sub(/\r$/, ""); print; exit }' \
                "$slash_headers"
            )"
            if [ "$location" != "$canonical_path" ]; then
              rm -f "$slash_headers" "$canonical_headers" "$canonical_body"
              echo "Direct origin ${slash_path} Location was ${location:-<missing>}; expected ${canonical_path}." >&2
              return 1
            fi

            if ! canonical_status="$(
              curl -sS -D "$canonical_headers" -o "$canonical_body" -w '%{http_code}' \
                -H "Host: $host" -H 'X-Forwarded-Proto: https' \
                "${origin}${canonical_path}"
            )"; then
              rm -f "$slash_headers" "$canonical_headers" "$canonical_body"
              echo "Direct origin ${canonical_path} did not return an HTTP response." >&2
              return 1
            fi
            if [[ "$canonical_status" != 2* ]] || \
               grep -qi '^Location:' "$canonical_headers" || \
               [ ! -s "$canonical_body" ]; then
              rm -f "$slash_headers" "$canonical_headers" "$canonical_body"
              echo "Direct origin ${canonical_path} failed the canonical content/no-loop contract." >&2
              return 1
            fi

            rm -f "$slash_headers" "$canonical_headers" "$canonical_body"
          }

          verify_direct_origin_route '/download/' '/download'
          verify_direct_origin_route '/help/' '/help'
          compose_cmd ps
          trap - ERR
          REMOTE

      - name: Verify staging direct-origin canonical routes
        shell: bash
        run: |
          set -euo pipefail
          ssh opshub-staging 'bash -s' <<'REMOTE'
          set -euo pipefail
          origin='http://127.0.0.1:8090'
          host='opshub-staging.hoanghochoi.com'

          verify_canonical_route() {
            slash_path="$1"
            canonical_path="$2"
            slash_headers="$(mktemp)"
            canonical_headers="$(mktemp)"
            canonical_body="$(mktemp)"
            trap 'rm -f "$slash_headers" "$canonical_headers" "$canonical_body"' RETURN

            slash_status="$(
              curl -sS -D "$slash_headers" -o /dev/null -w '%{http_code}' \
                -H "Host: $host" -H 'X-Forwarded-Proto: https' \
                "${origin}${slash_path}"
            )"
            if [ "$slash_status" != '308' ]; then
              echo "Direct origin ${slash_path} returned ${slash_status}; expected 308." >&2
              return 1
            fi
            location="$(
              awk 'tolower($1) == "location:" { $1=""; sub(/^[[:space:]]+/, ""); sub(/\r$/, ""); print; exit }' \
                "$slash_headers"
            )"
            if [ "$location" != "$canonical_path" ]; then
              echo "Direct origin ${slash_path} Location was ${location:-<missing>}; expected ${canonical_path}." >&2
              return 1
            fi

            canonical_status="$(
              curl -sS -D "$canonical_headers" -o "$canonical_body" -w '%{http_code}' \
                -H "Host: $host" -H 'X-Forwarded-Proto: https' \
                "${origin}${canonical_path}"
            )"
            if [[ "$canonical_status" != 2* ]]; then
              echo "Direct origin ${canonical_path} returned ${canonical_status}; expected 2xx content." >&2
              return 1
            fi
            if grep -qi '^Location:' "$canonical_headers"; then
              echo "Direct origin ${canonical_path} unexpectedly redirects." >&2
              return 1
            fi
            if [ ! -s "$canonical_body" ]; then
              echo "Direct origin ${canonical_path} returned an empty body." >&2
              return 1
            fi
          }

          verify_canonical_route '/download/' '/download'
          verify_canonical_route '/help/' '/help'
          REMOTE

      - name: Verify staging public health and version metadata
        shell: bash
        env:
          CF_ACCESS_CLIENT_ID: ${{ secrets.CF_ACCESS_CLIENT_ID }}
          CF_ACCESS_CLIENT_SECRET: ${{ secrets.CF_ACCESS_CLIENT_SECRET }}
        run: |
          set -euo pipefail
          if [[ -z "${CF_ACCESS_CLIENT_ID}" || -z "${CF_ACCESS_CLIENT_SECRET}" ]]; then
            echo "Cloudflare Access service credentials are required for staging verification." >&2
            exit 1
          fi
          access_curl() {
            local url="$1"
            shift
            if [[ "$url" == "${OPSHUB_PUBLIC_BASE_URL}"* ]]; then
              curl "$@" \
                -H "CF-Access-Client-Id: ${CF_ACCESS_CLIENT_ID}" \
                -H "CF-Access-Client-Secret: ${CF_ACCESS_CLIENT_SECRET}" \
                "$url"
              return
            fi
            curl "$@" "$url"
          }
          retry_curl() {
            local url="$1"
            local attempt
            local response_file
            local status
            for attempt in {1..12}; do
              response_file="$(mktemp)"
              if status="$(access_curl "$url" -sS -o "$response_file" -w '%{http_code}')"; then
                if [[ "$status" == 2* ]]; then
                  cat "$response_file"
                  rm -f "$response_file"
                  return 0
                fi
              fi
              rm -f "$response_file"
              sleep 5
            done
            echo "Staging verification did not receive a 2xx response from ${url}." >&2
            return 1
          }
          verify_security_headers() {
            local headers="$1"
            local header
            for header in \
              'content-security-policy:' \
              'strict-transport-security:.*max-age=31536000' \
              'x-content-type-options:.*nosniff' \
              'x-frame-options:.*sameorigin' \
              'referrer-policy:.*no-referrer' \
              'permissions-policy:'; do
              if ! printf '%s\n' "$headers" | grep -Eqi "^${header}"; then
                echo "Staging response is missing required security header pattern: ${header}" >&2
                return 1
              fi
            done
            if printf '%s\n' "$headers" | grep -qi '^content-security-policy-report-only:'; then
              echo 'Staging still serves a report-only CSP.' >&2
              return 1
            fi
          }

          retry_curl "${OPSHUB_PUBLIC_BASE_URL}/health"
          retry_curl "${OPSHUB_PUBLIC_BASE_URL}/" >/dev/null
          main_headers_file="$(mktemp)"
          if ! main_status="$(
            access_curl "${OPSHUB_PUBLIC_BASE_URL}/main.dart.js?v=${GITHUB_SHA}" \
              -sS -D "$main_headers_file" -o /dev/null -w '%{http_code}'
          )"; then
            rm -f "$main_headers_file"
            echo 'Staging main.dart.js request failed before receiving an HTTP response.' >&2
            exit 1
          fi
          main_headers="$(cat "$main_headers_file")"
          rm -f "$main_headers_file"
          if [[ "$main_status" != 2* ]]; then
            echo "Staging main.dart.js returned HTTP ${main_status}; expected 2xx." >&2
            exit 1
          fi
          if ! printf '%s\n' "$main_headers" | grep -qi 'cache-control:.*no-cache'; then
            echo 'Staging main.dart.js is missing the required no-cache policy.' >&2
            exit 1
          fi
          verify_security_headers "$main_headers"
          android_json="$(retry_curl "${OPSHUB_API_BASE_URL}/app-version?platform=android")"
          windows_json="$(retry_curl "${OPSHUB_API_BASE_URL}/app-version?platform=windows")"
          web_json="$(retry_curl "${OPSHUB_API_BASE_URL}/app-version?platform=web")"
          node -e "
            const android = JSON.parse(process.argv[1]);
            const windows = JSON.parse(process.argv[2]);
            const web = JSON.parse(process.argv[3]);
            const expectedBuild = Number(process.argv[4]);
            const publicBaseUrl = process.argv[5];
            for (const [platform, payload] of Object.entries({ android, windows, web })) {
              if (payload.latestBuild !== expectedBuild) {
                throw new Error(platform + ' latestBuild expected ' + expectedBuild + ', got ' + payload.latestBuild);
              }
            }
            for (const [platform, payload] of Object.entries({ android, windows })) {
              if (!String(payload.updateUrl || '').startsWith(publicBaseUrl + '/downloads/')) {
                throw new Error(platform + ' updateUrl does not point to staging downloads');
              }
              if (String(payload.updateUrl || '').includes('opshub.hoanghochoi.com/downloads/')) {
                throw new Error(platform + ' updateUrl points to production downloads');
              }
              if (payload.packageUrl !== payload.updateUrl) {
                throw new Error(platform + ' packageUrl must match updateUrl for self-update');
              }
              if (!/^[0-9a-f]{64}$/.test(String(payload.packageSha256 || ''))) {
                throw new Error(platform + ' packageSha256 is invalid');
              }
              if (!Number.isInteger(payload.packageSizeBytes) || payload.packageSizeBytes <= 0) {
                throw new Error(platform + ' packageSizeBytes is invalid');
              }
            }
            if (!String(android.updateUrl || '').endsWith('.apk')) throw new Error('Android updateUrl is not an APK');
            if (!String(windows.updateUrl || '').endsWith('.exe')) throw new Error('Windows updateUrl is not an installer EXE');
            if (android.packageType !== 'apk') throw new Error('Android packageType must be apk');
            if (windows.packageType !== 'windowsInstaller') throw new Error('Windows packageType must be windowsInstaller');
            if (!Array.isArray(windows.installerArgs) || !windows.installerArgs.includes('/VERYSILENT')) {
              throw new Error('Windows installerArgs must include /VERYSILENT');
            }
            if (!windows.installerArgs.includes('/OPSHUBRELAUNCH=1')) {
              throw new Error('Windows installerArgs must include /OPSHUBRELAUNCH=1');
            }
            if (web.platform !== 'web') throw new Error('Web app-version did not report platform=web');
            if (String(web.updateUrl || '') !== '') throw new Error('Web updateUrl must be empty');
            if (web.forceUpdate !== false) throw new Error('Web forceUpdate must be false');
            if (web.minSupportedBuild !== 1) throw new Error('Web minSupportedBuild must be 1');
          " "$android_json" "$windows_json" "$web_json" "${{ needs.prepare.outputs.version_code }}" "$OPSHUB_DOWNLOAD_PUBLIC_BASE_URL"
          curl -fIs "$(node -e "console.log(JSON.parse(process.argv[1]).packageUrl)" "$android_json")" >/dev/null
          curl -fIs "$(node -e "console.log(JSON.parse(process.argv[1]).packageUrl)" "$windows_json")" >/dev/null
          retry_curl "${OPSHUB_PUBLIC_BASE_URL}/download" >/dev/null
          download_redirect_headers="$(curl -fsSI "${OPSHUB_PUBLIC_BASE_URL}/download")"
          printf '%s\n' "$download_redirect_headers" |
            grep -qi '^www-authenticate: Cloudflare-Access '
          printf '%s\n' "$download_redirect_headers" |
            grep -Eqi '^location: https://[^/]+\.cloudflareaccess\.com/cdn-cgi/access/login/.*redirect_url=%2Fdownload'
          retry_curl "${OPSHUB_PUBLIC_BASE_URL}/help" >/dev/null
          help_json="$(retry_curl "${OPSHUB_API_BASE_URL}/help-content/public")"
          manifest_json="$(retry_curl "${OPSHUB_DOWNLOAD_PUBLIC_BASE_URL}/downloads/latest.json")"
          node -e "
            const help = JSON.parse(process.argv[1]);
            const manifest = JSON.parse(process.argv[2]);
            const expectedVersion = process.argv[3];
            const expectedBuild = Number(process.argv[4]);
            const publicBaseUrl = process.argv[5];
            if (!Array.isArray(help.pages) || help.pages.length === 0) {
              throw new Error('Public help runtime snapshot is empty on staging');
            }
            if (manifest.version !== expectedVersion) throw new Error('Manifest version mismatch');
            if (manifest.build !== expectedBuild) throw new Error('Manifest build mismatch');
            for (const [key, file] of Object.entries(manifest.files || {})) {
              if (!file?.url || !file?.fileName || !Number.isInteger(file.sizeBytes) || file.sizeBytes <= 0) {
                throw new Error('Invalid manifest file entry: ' + key);
              }
              if (!file.url.startsWith(publicBaseUrl + '/downloads/')) {
                throw new Error('Manifest file does not point to staging: ' + key);
              }
              if (file.url.includes('opshub.hoanghochoi.com/downloads/')) {
                throw new Error('Manifest file points to production downloads: ' + key);
              }
            }
          " "$help_json" "$manifest_json" "${{ needs.prepare.outputs.version_name }}" "${{ needs.prepare.outputs.version_code }}" "$OPSHUB_DOWNLOAD_PUBLIC_BASE_URL"
          node -e "
            const manifest = JSON.parse(process.argv[1]);
            for (const file of Object.values(manifest.files)) console.log(file.url);
          " "$manifest_json" | while IFS= read -r url; do
            curl -fIs "$url" >/dev/null
          done

          ws_headers_file="$(mktemp)"
          if ! ws_status="$(
            access_curl "${OPSHUB_PUBLIC_BASE_URL}/ws/v2" \
              --http1.1 -sS -D "$ws_headers_file" -o /dev/null -w '%{http_code}' \
              -H 'Connection: Upgrade' \
              -H 'Upgrade: websocket' \
              -H 'Sec-WebSocket-Version: 13' \
              -H 'Sec-WebSocket-Key: b3BzaHViLXN0YWdpbmctc21va2U='
          )"; then
            rm -f "$ws_headers_file"
            echo 'Staging /ws/v2 smoke failed before receiving an HTTP response.' >&2
            exit 1
          fi
          rm -f "$ws_headers_file"
          if [ "$ws_status" != '401' ]; then
            echo "Staging /ws/v2 without a one-time ticket returned ${ws_status}; expected 401 from the realtime gateway." >&2
            exit 1
          fi

      - name: Roll back staging after failed release verification
        if: ${{ (failure() || cancelled()) && steps.deploy_runtime.outcome == 'success' }}
        shell: bash
        run: |
          set -euo pipefail
          remote_release_dir="${OPSHUB_REMOTE_APP_DIR}/releases/${GITHUB_SHA}-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          client_staging_dir="${OPSHUB_REMOTE_APP_DIR}/action-staging/${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          tmp_dir="${OPSHUB_REMOTE_APP_DIR}/tmp/${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          ssh opshub-staging \
            "REMOTE_RELEASE_DIR='$remote_release_dir' \
             CURRENT_DIR='${OPSHUB_REMOTE_APP_DIR}/current' \
             CLIENT_STAGING_DIR='$client_staging_dir' \
             TMP_DIR='$tmp_dir' \
             OPSHUB_REMOTE_APP_DIR='${OPSHUB_REMOTE_APP_DIR}' \
             DEPLOY_RUN_ID='$GITHUB_RUN_ID' \
             OPSHUB_ENV_FILE='$OPSHUB_ENV_FILE' \
             OPSHUB_SSD_ROOT='$OPSHUB_SSD_ROOT' \
             DOWNLOADS_DIR='${OPSHUB_SSD_ROOT}/downloads' \
             WEB_DIR='${OPSHUB_SSD_ROOT}/web' \
             APK_NAME='${{ needs.prepare.outputs.apk_name }}' \
             WINDOWS_ZIP_NAME='${{ needs.prepare.outputs.windows_zip_name }}' \
             WINDOWS_INSTALLER_NAME='${{ needs.prepare.outputs.windows_installer_name }}' \
             WINDOWS_CHECKSUM_NAME='${{ needs.prepare.outputs.windows_checksum_name }}' \
             COMPOSE_PROJECT_NAME='$COMPOSE_PROJECT_NAME' \
             bash -s" <<'REMOTE'
          set -euo pipefail

          case "$DEPLOY_RUN_ID" in
            ''|*[!0-9]*)
              echo "Invalid staging rollback run id." >&2
              exit 1
              ;;
          esac

          env_snapshot="${OPSHUB_ENV_FILE}.rollback.${DEPLOY_RUN_ID}"
          rollback_state="${OPSHUB_SSD_ROOT}/rollback/deploy-${DEPLOY_RUN_ID}.state"
          shared_snapshot_dir="${OPSHUB_SSD_ROOT}/rollback/deploy-${DEPLOY_RUN_ID}.shared"
          shared_stage_dir="${OPSHUB_SSD_ROOT}/rollback/deploy-${DEPLOY_RUN_ID}.stage"
          if ! sudo test -s "$env_snapshot" || ! sudo test -s "$rollback_state"; then
            echo "Protected staging rollback env or release metadata is missing; manual recovery is required." >&2
            exit 1
          fi
          if ! sudo test -e "$shared_snapshot_dir/SNAPSHOT_READY" || \
             ! sudo test -e "$shared_snapshot_dir/PROMOTION_STARTED" || \
             ! sudo test -e "$shared_snapshot_dir/PROMOTED"; then
            echo "Protected staging shared-state snapshot is incomplete; refusing partial rollback." >&2
            exit 1
          fi
          rollback_state_lines="$(sudo wc -l "$rollback_state" | awk '{print $1}')"
          if [ "$rollback_state_lines" != '2' ]; then
            echo "Protected staging rollback metadata is malformed; refusing rollback." >&2
            exit 1
          fi

          previous_release="$(sudo sed -n '1p' "$rollback_state")"
          expected_release="$(sudo sed -n '2p' "$rollback_state")"
          releases_dir="$(readlink -f "$(dirname "$REMOTE_RELEASE_DIR")" || true)"
          expected_release_real="$(readlink -f "$REMOTE_RELEASE_DIR" || true)"
          current_real="$(readlink -f "$CURRENT_DIR" || true)"
          previous_real="$(readlink -f "$previous_release" || true)"

          case "$previous_real" in
            "$releases_dir"/*) ;;
            *)
              echo "Rollback release is outside the protected releases directory." >&2
              exit 1
              ;;
          esac
          if [ ! -d "$previous_real" ] || \
             [ "$expected_release" != "$expected_release_real" ] || \
             [ "$current_real" != "$expected_release_real" ]; then
            echo "Staging rollback metadata no longer matches the active release; refusing to change runtime state." >&2
            exit 1
          fi

          if [ "$DOWNLOADS_DIR" != "${OPSHUB_SSD_ROOT}/downloads" ] || \
             [ "$WEB_DIR" != "${OPSHUB_SSD_ROOT}/web" ]; then
            echo "Shared rollback paths are outside OPSHUB_SSD_ROOT." >&2
            exit 1
          fi
          for artifact_name in "$APK_NAME" "$WINDOWS_ZIP_NAME" "$WINDOWS_INSTALLER_NAME" "$WINDOWS_CHECKSUM_NAME"; do
            if [ -z "$artifact_name" ] || [ "$(basename -- "$artifact_name")" != "$artifact_name" ]; then
              echo "Invalid rollback artifact name." >&2
              exit 1
            fi
          done

          restore_shared_publication() {
            local backup_path live_path name
            sudo rm -rf -- "$WEB_DIR" || return 1
            if sudo test -e "$shared_snapshot_dir/web"; then
              sudo cp -a --reflink=auto -- "$shared_snapshot_dir/web" "$WEB_DIR" || return 1
            fi

            sudo rm -rf -- "$DOWNLOADS_DIR/help" || return 1
            if sudo test -e "$shared_snapshot_dir/downloads/help"; then
              sudo cp -a --reflink=auto -- "$shared_snapshot_dir/downloads/help" "$DOWNLOADS_DIR/help" || return 1
            fi

            for name in latest.json download.html opshub-icon-192.png; do
              live_path="$DOWNLOADS_DIR/$name"
              backup_path="$shared_snapshot_dir/downloads/$name"
              sudo rm -f -- "$live_path" || return 1
              if sudo test -e "$backup_path"; then
                sudo cp -a --reflink=auto -- "$backup_path" "$live_path" || return 1
              fi
            done
            for name in "$APK_NAME" "$WINDOWS_ZIP_NAME" "$WINDOWS_INSTALLER_NAME" "$WINDOWS_CHECKSUM_NAME"; do
              live_path="$DOWNLOADS_DIR/$name"
              backup_path="$shared_snapshot_dir/versioned/$name"
              sudo rm -f -- "$live_path" || return 1
              if sudo test -e "$backup_path"; then
                sudo cp -a --reflink=auto -- "$backup_path" "$live_path" || return 1
              fi
            done
            if ! sudo test -e "$shared_snapshot_dir/DOWNLOADS_DIR_PRESENT"; then
              sudo rmdir -- "$DOWNLOADS_DIR" || return 1
            fi
          }

          restore_tmp="${OPSHUB_ENV_FILE}.restore.${DEPLOY_RUN_ID}"
          sudo cp --preserve=mode,ownership,timestamps -- "$env_snapshot" "$restore_tmp"
          sudo mv -Tf -- "$restore_tmp" "$OPSHUB_ENV_FILE"

          shared_restored=false
          if restore_shared_publication; then
            shared_restored=true
          else
            echo "Staging shared-state restore failed; continuing runtime rollback and retaining all checkpoints." >&2
          fi

          ln -sfn "$previous_real" "${CURRENT_DIR}.rollback"
          mv -Tf "${CURRENT_DIR}.rollback" "$CURRENT_DIR"
          cd "$CURRENT_DIR"
          export COMPOSE_PROJECT_NAME OPSHUB_ENV_FILE
          compose=(docker compose --env-file "$OPSHUB_ENV_FILE" -f deploy/home-server/docker-compose.home.yml)
          compose_cmd() {
            "${compose[@]}" "$@" < /dev/null
          }
          if ! compose_cmd up -d --build --force-recreate --wait --wait-timeout 240 redis api realtime caddy; then
            compose_cmd ps || true
            compose_cmd logs --tail=200 api || true
            compose_cmd logs --tail=120 realtime || true
            compose_cmd logs --tail=120 caddy || true
            echo "Staging rollback runtime did not become healthy; snapshot and metadata were retained." >&2
            exit 1
          fi
          curl -fsS \
            -H 'Host: opshub-staging.hoanghochoi.com' \
            -H 'X-Forwarded-Proto: https' \
            'http://127.0.0.1:8090/health' >/dev/null
          if [ "$shared_restored" != true ]; then
            echo "Prior runtime is healthy, but shared-state rollback requires manual recovery." >&2
            exit 1
          fi
          case "$CLIENT_STAGING_DIR" in
            "$OPSHUB_REMOTE_APP_DIR"/action-staging/*) rm -rf -- "$CLIENT_STAGING_DIR" ;;
            *) echo "Refusing to remove unexpected staged client directory." >&2; false ;;
          esac
          case "$TMP_DIR" in
            "$OPSHUB_REMOTE_APP_DIR"/tmp/*) rm -rf -- "$TMP_DIR" ;;
            *) echo "Refusing to remove unexpected staged static directory." >&2; false ;;
          esac
          sudo rm -f -- "$env_snapshot" "$rollback_state"
          sudo rm -rf -- "$shared_stage_dir" "$shared_snapshot_dir"
          echo "Staging release, env, web, Help, manifest and client files were rolled back after failed public verification." >&2
          REMOTE

      - name: Finalize successful staging rollback checkpoint
        if: ${{ success() && steps.deploy_runtime.outcome == 'success' }}
        shell: bash
        run: |
          set -euo pipefail
          remote_release_dir="${OPSHUB_REMOTE_APP_DIR}/releases/${GITHUB_SHA}-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          client_staging_dir="${OPSHUB_REMOTE_APP_DIR}/action-staging/${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          tmp_dir="${OPSHUB_REMOTE_APP_DIR}/tmp/${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          ssh opshub-staging \
            "REMOTE_RELEASE_DIR='$remote_release_dir' \
             CURRENT_DIR='${OPSHUB_REMOTE_APP_DIR}/current' \
             CLIENT_STAGING_DIR='$client_staging_dir' \
             TMP_DIR='$tmp_dir' \
             OPSHUB_REMOTE_APP_DIR='${OPSHUB_REMOTE_APP_DIR}' \
             DEPLOY_RUN_ID='$GITHUB_RUN_ID' \
             OPSHUB_ENV_FILE='$OPSHUB_ENV_FILE' \
             OPSHUB_SSD_ROOT='$OPSHUB_SSD_ROOT' \
             bash -s" <<'REMOTE'
          set -euo pipefail
          env_snapshot="${OPSHUB_ENV_FILE}.rollback.${DEPLOY_RUN_ID}"
          rollback_state="${OPSHUB_SSD_ROOT}/rollback/deploy-${DEPLOY_RUN_ID}.state"
          shared_snapshot_dir="${OPSHUB_SSD_ROOT}/rollback/deploy-${DEPLOY_RUN_ID}.shared"
          shared_stage_dir="${OPSHUB_SSD_ROOT}/rollback/deploy-${DEPLOY_RUN_ID}.stage"
          expected_release="$(sudo sed -n '2p' "$rollback_state")"
          previous_release="$(sudo sed -n '1p' "$rollback_state")"
          current_real="$(readlink -f "$CURRENT_DIR" || true)"
          remote_release_real="$(readlink -f "$REMOTE_RELEASE_DIR" || true)"
          previous_real="$(readlink -f "$previous_release" || true)"
          if ! sudo test -s "$env_snapshot" || \
             ! sudo test -s "$rollback_state" || \
             ! sudo test -e "$shared_snapshot_dir/SNAPSHOT_READY" || \
             ! sudo test -e "$shared_snapshot_dir/PROMOTION_STARTED" || \
             ! sudo test -e "$shared_snapshot_dir/PROMOTED" || \
             [ "$expected_release" != "$remote_release_real" ] || \
             [ "$current_real" != "$remote_release_real" ]; then
            echo "Staging rollback checkpoint is incomplete or does not match the verified release." >&2
            exit 1
          fi

          case "$CLIENT_STAGING_DIR" in
            "$OPSHUB_REMOTE_APP_DIR"/action-staging/*) rm -rf -- "$CLIENT_STAGING_DIR" ;;
            *) echo "Refusing to remove unexpected staged client directory." >&2; false ;;
          esac
          case "$TMP_DIR" in
            "$OPSHUB_REMOTE_APP_DIR"/tmp/*) rm -rf -- "$TMP_DIR" ;;
            *) echo "Refusing to remove unexpected staged static directory." >&2; false ;;
          esac

          releases_dir="$(dirname "$REMOTE_RELEASE_DIR")"
          case "$previous_real" in
            "$releases_dir"/*) ;;
            *) echo "Previous staging release is outside the protected releases directory." >&2; exit 1 ;;
          esac
          mapfile -t release_dirs < <(find "$releases_dir" -mindepth 1 -maxdepth 1 -type d -printf '%T@ %p\n' | sort -rn | awk '{print $2}')
          kept=0
          for dir in "${release_dirs[@]}"; do
            dir_real="$(readlink -f "$dir")"
            if [ "$dir_real" = "$current_real" ] || \
               [ "$dir_real" = "$previous_real" ] || \
               [ "$kept" -lt 5 ]; then
              kept=$((kept + 1))
              continue
            fi
            rm -rf -- "$dir"
          done

          downloads_dir="$OPSHUB_SSD_ROOT/downloads"
          mapfile -t client_files < <(sudo find "$downloads_dir" -maxdepth 1 -type f \( -name 'phongvu-opshub-staging-v*.apk' -o -name 'phongvu-opshub-staging-windows-v*.zip' -o -name 'phongvu-opshub-staging-windows-setup-v*.exe' -o -name 'phongvu-opshub-staging-windows-v*.sha256' \) -printf '%T@ %p\n' | sort -rn | awk '{print $2}')
          kept=0
          for file in "${client_files[@]}"; do
            if [ "$kept" -lt 20 ]; then
              kept=$((kept + 1))
              continue
            fi
            sudo rm -f -- "$file"
          done

          sudo rm -f -- "$rollback_state" "$env_snapshot"
          sudo rm -rf -- "$shared_stage_dir" "$shared_snapshot_dir"
          REMOTE
